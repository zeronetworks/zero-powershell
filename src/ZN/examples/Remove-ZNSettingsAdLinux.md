### Example 1: Remove the Linux settings from AD
```powershell
Remove-ZNSettingsAdLinux -ForestId f:a:0bec706b -DomainId posh.local
```

```output

```

This cmdlet removes the Linux settings from AD domain/forest.
